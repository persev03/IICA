{
  "name": "iica",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "pnpm --filter @iica/web dev",
    "build": "pnpm --filter @iica/web build",
    "lint": "pnpm --filter @iica/web lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "pnpm --filter @iica/iica-engine test"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
